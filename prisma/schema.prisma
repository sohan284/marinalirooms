generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model PageContent {
  id        String   @id @default(cuid())
  page      String   // e.g., "home"
  lang      String   // e.g., "bn"
  sections  Json     // Holds hero, features, footer
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())

  @@unique([page, lang])
}

model SiteSettings {
  id              String   @id @default("default")
  primaryColor    String   @default("#123149")
  secondaryColor  String   @default("#F2DE97")
  backgroundColor String   @default("#F8F6F2")
  textColor       String   @default("#111111")
  fontFamily      String   @default("var(--font-playfair)")
  logo            String?
  logoKey         String?
  heroImage       String?
  heroImageKey    String?
  retreatImage    String?
  retreatImageKey String?
  footerConfig    Json?
  updatedAt       DateTime @updatedAt
  createdAt       DateTime @default(now())
}

model Room {
  id           String   @id @default(cuid())
  slug         String   @unique
  images       Json?    @default("[]") // Array of image objects { url, key }
  translations Json     // { it: { name, location, description, capacity, amenities }, en: { ... }, de: { ... } }
  order        Int      @default(0)
  updatedAt    DateTime @updatedAt
  createdAt    DateTime @default(now())
}model Admin {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
